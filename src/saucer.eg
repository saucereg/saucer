require:
   ;;underscore -> extend
   jquery as $
   ./events -> Events
provide: Saucer, Events

class Saucer < Events:
   static:
      $ = $
      Events = Events

;;saucer = Saucer()
;;saucer.on(.expand) with -> print "Expanded!"
;;saucer.trigger(.expand)
